# v0.10 — systemic pattern progression (a NeetCode-shaped roadmap)

Status: ✅ delivered (machinery milestone; content batches follow as v0.10.x).

## What shipped (v0.10.0)

- `data/roadmap.toml` — the NeetCode 150 structure vendored from the public mirror with a provenance header + a dojo addendum (the mirror lists 149; the 150th, 0271 encode-and-decode-strings, was appended to Arrays & Hashing — dojo already owns it).
- `patterns.py` — the 18 groups in roadmap order, `GROUP_SLUGS` (display-name mapping), `PREREQS` (a chain over the verified group order — dojo's documented reading; the mirror carries the order, not the site's DAG edges), and the TAG_TO_PATTERN updates (trie→tries, interval→intervals, dp tags→dp_1d, graph→graphs, math→math_and_geometry).
- `roadmap.py` — TOML parser (fail-loudly on unknown groups / malformed ids) + `next_ladder_problem`.
- Re-bucketed seed corpus onto the 18 directories (math→math_and_geometry; sum_largest_contiguous→greedy; generate_sample_to_target_sum→dp_2d; new empty groups with .gitkeep), `problems.lc_number` column (additive) + importer support + 29 override entries.
- Scheduler: `pick_new_problem` walks the roadmap under the hard prereq gate and serves the earliest unsolved ladder problem of the first eligible pattern (uncurated rows invisible to the ladder; dojo's own problems are the fallback); `pick_practice_problem` respects ladder order; `ladder_state` shared with the view.
- `dojo roadmap` — visible command rendering the tree (✓ complete · → next up · locked — finish X) + next-up footer.
- `MAJOR_VERSION` → "0.10" (the debug log cleared on the next real run — the designed mechanism).

Judgment call flagged for review: the prereq edges are a chain over the verified group order (each group unlocks after the previous is complete). The mirror carries the order, not the site's explicit DAG edges, and inventing edges would be unverified adaptation — the chain is the faithful reading. Adjusting it is a one-line change in `patterns.PREREQS`.

Open content: ~126 of the 150 problems are not yet fetched; the first content batch (sliding_window, per the plan) is the next milestone.

## Settled decisions

1. **Adopt NeetCode's 18 groups exactly.** dojo's pattern set becomes (in NeetCode's order): `arrays_and_hashing`, `two_pointers`, `sliding_window`, `stack`, `binary_search`, `linked_list`, `trees`, `tries`, `heap` (≈ "Heap / Priority Queue"), `backtracking`, `graphs`, `advanced_graphs`, `dp_1d`, `dp_2d`, `greedy`, `intervals`, `math_and_geometry`, `bit_manipulation`. The `PATTERNS` tuple takes this order, so the `dojo learn` picker reads systemically.
2. **Hard prereq gate.** The scheduler never serves a problem whose pattern has unsatisfied prerequisites; explicit `dojo <slug>` stays exempt (the deliberate-choice precedent).
3. **Machinery + one complete group per milestone.** Graph/scheduler/roadmap-view first, then each group's ladder top-down (sliding window, binary search, linked list, …), each problem through the existing fetch + dual-oracle curation + verification gate.
4. **`dojo roadmap` tree view** ships in v0.10 (solved/unsolved/next-up, prereq state).
5. **Provenance:** the vendored list lives in `data/roadmap.toml` with a header crediting the NeetCode 150 structure; dojo's own hand-written problems (correlation, array_intersection, …) stay in their groups outside the canonical ladder.

## The re-bucketing migration (verified: nearly free)

The bank importer upserts by slug and writes `pattern = excluded.pattern` (the directory name), so moving problem files across directories re-buckets rows on the next `ensure_seeded` — no hand-edited SQL. Live-DB check: `pattern_cards` holds only arrays_and_hashing/heap/stack, and `learn_sessions` only arrays_and_hashing — **zero scheduling/learning rows** reference a renamed or split pattern. What actually moves:

- `problems/math/` (2 problems) → `problems/math_and_geometry/`
- `problems/graph/` (0 problems) → `problems/graphs/`
- `problems/dynamic_programming/` (1 problem: generate_sample_to_target_sum, a target-sum variant) → `problems/dp_2d/` (NeetCode buckets target_sum under 2-D DP)
- new empty dirs: `tries`, `intervals`, `advanced_graphs`, `dp_1d`

Attempt rows join problems on `problem_id`, so history follows automatically. `patterns.py` gains the 18-name tuple in NeetCode order plus the prereq edges and the TAG_TO_PATTERN additions (trie → tries, interval → intervals, union-find → advanced_graphs stays in graphs? — decide at build: union-find currently maps to `graph`; with the split, union-find/topological/shortest-path stay `graphs`, and nothing new maps to `advanced_graphs` automatically — advanced problems are curated by hand).

## The diagnosis

dojo's pattern taxonomy is fine-grained but **flat and thin**: 14 patterns with no ordering between them, no ordering within them, and a bank whose coverage collapses after the first three techniques. Measured against the canonical NeetCode 150 list (the de-facto standard ladder):

| NeetCode group | dojo coverage |
|---|---|
| Arrays & Hashing | 8/8 ✓ |
| Two Pointers | 5/5 ✓ |
| Stack | 7/7 ✓ |
| Sliding Window | 0/6 |
| Binary Search | 0/7 |
| Linked List | 0/11 |
| Trees | 2/15 |
| Tries | 0/3 (no such pattern) |
| Heap / Priority Queue | 1/7 |
| Backtracking | 0/9 |
| Graphs | 0/13 |
| Advanced Graphs | 0/6 (folded into graph) |
| 1-D DP | 0/12 |
| 2-D DP | 0/11 (folded into dynamic_programming) |
| Greedy | 1/8 |
| Intervals | 0/6 (no such pattern) |
| Math & Geometry | 0/8 |
| Bit Manipulation | 0/7 |

**24/150 covered** (plus ~8 problems outside the 150). The scheduler's "weakest pattern first" then makes it worse: it happily serves whatever exists, with no notion that a pattern has prerequisites or that a pattern's problems form a ladder. The result feels random — correctly.

## What the NeetCode tree teaches (design lessons, not just a list)

1. **A progression graph, not a flat taxonomy.** The roadmap is 18 technique groups in prerequisite order (arrays → two pointers → sliding window → stack → binary search → linked list → trees → tries → heap → backtracking → graphs → advanced graphs → 1-D DP → 2-D DP → greedy → intervals → math & geometry → bit manipulation). Sliding window before two pointers would be pedagogically wrong; the order *is* the system.
2. **Each group is a difficulty ladder, and each problem teaches one new trick.** Arrays & Hashing: contains_duplicate (set) → valid_anagram (counting) → two_sum (complement map) → group_anagrams (canonical key) → top_k (bucket/frequency) → product_except_self (prefix/suffix) → valid_sudoku (indexing) → longest_consecutive (set + streaks). Every next problem is the *least novel* unsolved problem — minimal marginal difficulty.
3. **Coverage follows a power law.** Trees 15, graphs 13+6, DP 12+11 — the core techniques get the most reps; support techniques (tries 3, intervals 6, bit 7) get enough to establish the pattern.
4. **dojo's missing dimension is *order*, and its missing content is *breadth*.** The taxonomy needs prereq edges + per-pattern problem sequences; the bank needs the ~126 missing free problems (which dojo's existing `fetch` + curator pipeline can produce — the machinery already exists).

## The data question: API and scraping

- **NeetCode has no official public API.** The site is a Next.js app with unversioned internal endpoints; relying on them is fragile and against the ToS. Scraping neetcode.io is both unnecessary and disallowed by its robots/ToS.
- **The roadmap *list* needs no scraping.** The free NeetCode 150 structure is public knowledge and cleanly mirrored in community repos (e.g. [lufftw/neetcode](https://github.com/lufftw/neetcode) ships it as a `roadmaps/neetcode_150.toml`: 18 ordered groups × ordered LeetCode numbers — verified against the live site). Vendoring that list into the repo (with provenance noted) is consistent with the private-use policy.
- **The problem *content* already has a legal-for-private-use path**: NeetCode displays LeetCode's own statements, and dojo already fetches those through its injected GraphQL client (`dojo fetch <slug>`) with the HTML→text converter and auto-curation + verification gate. So the pipeline is: **vendored roadmap list → `dojo fetch` per slug → curator → judge registrations**, entirely through dojo's existing machinery. Zero scraping of neetcode.io.

## The proposal

1. **Progression graph in the taxonomy** (`patterns.py` + a new data file): prerequisite edges between patterns (derived from the NeetCode group order) and a canonical *ordered problem sequence* per pattern (vendored `data/roadmap.toml`, mapping NeetCode group → ordered LeetCode numbers, with a `dojo`-side mapping slug ↔ LC number stored on problem rows).
2. **Scheduler respects the graph.** `pick_new_problem` becomes: walk patterns in topological order; the next problem is the earliest unsolved problem in the first unsatisfied pattern whose prerequisites are all satisfied. Stability/weakest-first becomes the tiebreak *within* the eligible frontier. Explicit `dojo <slug>` stays exempt (the deliberate-choice precedent from the proactive offer).
3. **`dojo roadmap` view.** The tree rendered in the terminal with solved/unsolved/next-up markers, per-pattern progress, and prereq state — the "third way" surface for the new system.
4. **Content acquisition in batches.** Machinery first, then complete each group's ladder top-down from Arrays & Hashing (already 8/8) through the 150, one batch per milestone, each problem through the existing fetch + dual-oracle curation + verification gate (the gate already makes bulk import safe). Batch size is a session's worth of curation runs; `MAJOR_VERSION` bumps and log clearing ride the ship of v0.10.0.
5. **Migrations stay additive.** New patterns (tries, intervals, and optionally the 1-D/2-D DP and advanced-graphs splits) are new rows/tags; existing attempt rows keep their pattern values — but see Open decisions on renaming.

## Build order

1. **Data + taxonomy.** Vendor `data/roadmap.toml` (provenance header) → `patterns.py`: 18-name tuple in order + `PREREQS` edges + tests → re-bucket the seed corpus (the directory moves above) → add `lc_number` + per-pattern `sequence` to problems (additive migration) + importer reads them.
2. **Scheduler.** `pick_new_problem` walks patterns in topological order and serves the earliest unsolved problem in the first prereq-satisfied pattern; stability is the tiebreak within the eligible frontier. `pick_practice_problem` (learn-mode handoff) respects sequence order too. Tests.
3. **`dojo roadmap`.** The tree with solved/unsolved/next-up markers + prereq state; hidden from `--help` if secondary (decide at build; likely visible).
4. **First content batch.** Complete `sliding_window` (6 problems) through `dojo fetch` + curation, then `binary_search`, `linked_list`, … one group per milestone; verification-gate reports as evidence.
5. **Ship.** Docs (README progression section, architecture), `MAJOR_VERSION` → "0.10" (debug log clears — the intended mechanism), roadmap index.
